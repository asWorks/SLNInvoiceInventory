<UserControl x:Class="InvoiceInventory.Views.TestView"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006" 
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008" 
             xmlns:local="clr-namespace:InvoiceInventory"
             xmlns:syncfusion="http://schemas.syncfusion.com/wpf"
             mc:Ignorable="d" 
             d:DesignHeight="450" d:DesignWidth="800"
             >
    <UserControl.Resources>
        <ResourceDictionary>
            <ResourceDictionary.MergedDictionaries>
                <ResourceDictionary Source="/InvoiceInventory;component/Style/Style.xaml" />
            </ResourceDictionary.MergedDictionaries>
          
        </ResourceDictionary>
    </UserControl.Resources>
    <Grid>
        <Grid>
            <Grid.RowDefinitions>
                <RowDefinition Height="240*"/>
                <RowDefinition Height="240*"/>
            </Grid.RowDefinitions>
            <!--<DataGrid x:Name="Items" />-->
            <syncfusion:SfDataGrid x:Name="SyncfusionGrid"
                                       AutoGenerateColumns="False"
                                       BorderThickness="0"
                                       CellStyle="{StaticResource CellStyle}"
                                       ColumnSizer="Star"
                                       Background="White"
                                       HeaderRowHeight="25"
                                       ItemsSource="{Binding Items}"
                                       NavigationMode="Row"
                                       SelectionMode="Single"
                                       RowHeight="25"
                                   SelectedItem="{Binding SelectedItem}">
                <syncfusion:SfDataGrid.Columns>
                    <syncfusion:GridTextColumn MappingName="Datum" HorizontalHeaderContentAlignment="Right" TextAlignment="Right" HeaderText="Datum" ValueBinding="{Binding Path=Datum, StringFormat='MMM dd yyyy'}" />

                    <syncfusion:GridTextColumn HeaderText="RechnungsId" MappingName="RechnungsId" />
                    <syncfusion:GridTextColumn HeaderText="RechnungsNummer" MappingName="RechnungsNummer" />
                  
                    <syncfusion:GridTextColumn MappingName="´Netto" CellStyle="{StaticResource AmountCellStyle}" HorizontalHeaderContentAlignment="Right" TextAlignment="Right" ValueBinding="{Binding Path=Netto, StringFormat='{}{0:c}'}" />
                    <syncfusion:GridTextColumn MappingName="Zuzahlung" CellStyle="{StaticResource AmountCellStyle}" HorizontalHeaderContentAlignment="Right" TextAlignment="Right" ValueBinding="{Binding Path=Zuzahlung, StringFormat='{}{0:c}'}" />
                    <syncfusion:GridCheckBoxColumn MappingName="IstStorniert" HeaderText="IstStorniert"/>
                    <syncfusion:GridCheckBoxColumn MappingName="IstAusgebucht" HeaderText="IstAusgebucht"/>

                </syncfusion:SfDataGrid.Columns>

            </syncfusion:SfDataGrid>
            <StackPanel Grid.Row="2">
                <TextBox Text="{Binding SelectedItem.RechnungsNummer, Mode=OneWay}"/>
            </StackPanel>

        </Grid>

    </Grid>
</UserControl>
